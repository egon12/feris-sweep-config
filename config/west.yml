manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
    # Additional modules containing boards/shields/custom code can be listed here as well
    # See https://docs.zephyrproject.org/3.2.0/develop/west/manifest.html#projects
  projects:
    - name: zmk
      remote: zmkfirmware
      # Pinned to the ZMK `main` commit used by the last verified-working
      # CI build (2026-01-10, "change POWER into PWR", board: nice_nano).
      # Bump deliberately (and re-test on hardware), never track `main`.
      revision: 19582174f3a446a07d40f479020b4ebb1aad8ddb
      import: app/west.yml
  self:
    path: config
